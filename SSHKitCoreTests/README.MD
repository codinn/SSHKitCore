## initial you test environment

1. create user `sshtest`.

	> ./initTestEnv.sh

1. enable password authentication. TODO

1. open `System Perferences - Sharing`, enable `Remote Login` and add `SSH Test` to `Allow access for`. 